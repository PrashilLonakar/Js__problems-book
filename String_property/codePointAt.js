let text = "HELLO WORLD";
let code = text.codePointAt(3);

console.log('code',code);


// codePointAt() returns the code point at a specified position in a string.

// Get the code point at the first character:

// 76